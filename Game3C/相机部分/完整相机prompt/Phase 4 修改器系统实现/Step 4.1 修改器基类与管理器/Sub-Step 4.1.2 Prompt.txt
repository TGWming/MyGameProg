Sub-Step 4.1.2 Prompt

================================================================================
任务：创建CameraModifierBase. cpp - 修改器抽象基类实现
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4. 27 C++项目。
实现修改器基类的生命周期管理和通用功能。
这是所有26个修改器的核心实现基础。

【修改器生命周期】
```
Trigger() 调用
     ↓
┌────────────────────────────────────────────────────────┐
│  Inactive → BlendIn → Active → BlendOut → Inactive    │
│     ↑          │         │         │          │       │
│     │      StateTime  StateTime  StateTime    │       │
│     │      0→BlendIn  0→Duration 0→BlendOut   │       │
│     │          ↓         ↓         ↓          │       │
│     │      Weight     Weight=1   Weight       │       │
│     │      0→1                   1→0          │       │
│     └──────────────────────────────────────────┘       │
│                                                        │
│  Stop(false) → 进入BlendOut                            │
│  Stop(true)  → 直接进入Inactive                        │
└────────────────────────────────────────────────────────┘
```

【当前进度】
Phase 4 Step 4. 1 Sub-Step 4.1.2 (共190个Sub-Step中的第109个)
前置完成：4.1.1 CameraModifierBase. h ✅

【任务目标】
创建文件：Source/[ProjectName]/Private/Camera/Modifiers/CameraModifierBase.cpp
实现修改器基类的生命周期管理和通用功能。

【⚠️ 重要提醒】

1. **必须Regenerate：**
   本Sub-Step创建新的. cpp文件。
   创建文件后：
   - 保存所有文件
   - 右键. uproject → Generate Visual Studio project files
   - 重新加载解决方案
   - 然后编译

2. **编码格式要求：**
   - 文件编码：UTF-8 Without BOM
   - 所有注释：英文

3. **枚举值匹配：**
   确保EModifierState的枚举值名称与. h文件一致：
   - Inactive, BlendIn, Active, BlendOut
   （注意：不是BlendingIn/BlendingOut）

【具体要求】

1. **文件路径：** Source/[ProjectName]/Private/Camera/Modifiers/CameraModifierBase.cpp

2. **完整文件内容：**

```cpp
// Copyright Epic Games, Inc.  All Rights Reserved. 

#include "Camera/Modifiers/CameraModifierBase.h"
#include "Camera/Core/SoulsCameraManager.h"

/**
 * CameraModifierBase. cpp
 * 
 * Implementation of the abstract base class for all camera modifiers. 
 * Provides lifecycle management, blend weight calculation, and output generation.
 * 
 * Key Responsibilities:
 * - Manage modifier state transitions (Inactive → BlendIn → Active → BlendOut → Inactive)
 * - Calculate blend weight based on current state and timing
 * - Provide output structure with computed effect values
 * - Call virtual hooks for derived classes to implement specific effects
 * 
 * Derived classes MUST override:
 * - GetModifierType()
 * - GetModifierCategory()
 * - ComputeEffect() (to calculate actual effect values)
 */


//========================================
// Constructor
//========================================

UCameraModifierBase::UCameraModifierBase()
	: CurrentState(EModifierState::Inactive)
	, CurrentWeight(0.0f)
	, StateTime(0.0f)
	, TotalActiveTime(0.0f)
	, ActiveDuration(0.0f)
	, CurrentIntensity(1.0f)
	, PositionEffect(FVector:: ZeroVector)
	, RotationEffect(FRotator::ZeroRotator)
	, DistanceEffect(0.0f)
	, FOVEffect(0.0f)
	, VignetteEffect(0.0f)
	, ColorTintEffect(FLinearColor::White)
	, SaturationEffect(1.0f)
	, TimeDilationEffect(1.0f)
	, CameraManager(nullptr)
{
}


//========================================
// Modifier Identity
//========================================

FName UCameraModifierBase::GetModifierName() const
{
	// Default:  return class name
	return GetClass()->GetFName();
}

FString UCameraModifierBase::GetModifierDescription() const
{
	return FString::Printf(TEXT("Camera Modifier: %s"), *GetModifierName().ToString());
}


//========================================
// Lifecycle Control
//========================================

/**
 * Trigger - Activate this modifier
 * 
 * This is the main entry point for activating a modifier.
 * It stores trigger data, resets effect values, and begins the blend in phase.
 * 
 * @param InTriggerData Configuration data for this activation
 */
void UCameraModifierBase::Trigger(const FModifierTriggerData& InTriggerData)
{
	// Store trigger data
	TriggerData = InTriggerData;
	CurrentIntensity = FMath::Max(0.0f, TriggerData. Intensity);
	
	// Determine duration (use override if provided, otherwise default)
	if (TriggerData. DurationOverride > 0.0f)
	{
		ActiveDuration = TriggerData. DurationOverride;
	}
	else
	{
		ActiveDuration = GetDefaultDuration();
	}
	
	// Reset effect values to defaults
	ResetEffectValues();
	
	// Reset timing
	TotalActiveTime = 0.0f;
	
	// Start blend in phase
	TransitionToState(EModifierState::BlendIn);
	
	// Call derived class handler
	OnTriggered(TriggerData);
}

/**
 * Stop - Deactivate this modifier
 * 
 * @param bImmediate If true, skip blend out and go directly to Inactive
 */
void UCameraModifierBase:: Stop(bool bImmediate)
{
	// Already inactive, nothing to do
	if (CurrentState == EModifierState::Inactive)
	{
		return;
	}
	
	if (bImmediate)
	{
		// Immediately go to inactive
		TransitionToState(EModifierState::Inactive);
		OnDeactivated();
	}
	else
	{
		// Begin blend out (unless already blending out)
		if (CurrentState != EModifierState::BlendOut)
		{
			TransitionToState(EModifierState::BlendOut);
			OnDeactivating();
		}
	}
}

/**
 * Update - Update modifier state each frame
 * 
 * This method: 
 * 1. Updates state timing
 * 2. Checks for state transitions
 * 3. Calculates blend weight
 * 4. Calls ComputeEffect for derived class to calculate effects
 * 
 * @param DeltaTime Time since last update
 * @param Context Current camera execution context
 */
void UCameraModifierBase::Update(float DeltaTime, const FStageExecutionContext& Context)
{
	// Skip if inactive
	if (CurrentState == EModifierState::Inactive)
	{
		return;
	}
	
	// Update timing
	StateTime += DeltaTime;
	TotalActiveTime += DeltaTime;
	
	// Check for state transitions based on timing
	switch (CurrentState)
	{
		case EModifierState:: BlendIn: 
		{
			float BlendInTime = GetBlendInTime();
			if (BlendInTime <= 0.0f || StateTime >= BlendInTime)
			{
				// Blend in complete, transition to Active
				TransitionToState(EModifierState::Active);
				OnActivated();
			}
			break;
		}
		
		case EModifierState::Active: 
		{
			// Check if duration has elapsed (0 = infinite)
			if (ActiveDuration > 0.0f && StateTime >= ActiveDuration)
			{
				// Duration complete, begin blend out
				TransitionToState(EModifierState::BlendOut);
				OnDeactivating();
			}
			break;
		}
		
		case EModifierState::BlendOut:
		{
			float BlendOutTime = GetBlendOutTime();
			if (BlendOutTime <= 0.0f || StateTime >= BlendOutTime)
			{
				// Blend out complete, go inactive
				TransitionToState(EModifierState::Inactive);
				OnDeactivated();
			}
			break;
		}
		
		default:
			break;
	}
	
	// Calculate current blend weight
	CurrentWeight = CalculateBlendWeight();
	
	// Compute effect values (derived class implementation)
	if (CurrentState != EModifierState:: Inactive)
	{
		ComputeEffect(DeltaTime, Context);
	}
}


//========================================
// Output
//========================================

/**
 * GetOutput - Get the current modifier output
 * 
 * Fills the output structure with current effect values and state information.
 * The weight has already been calculated in Update().
 * 
 * @param Context Current camera execution context
 * @param OutOutput Output structure to fill
 * @return True if modifier has valid output (is active)
 */
bool UCameraModifierBase::GetOutput(const FStageExecutionContext& Context, FModifierOutput& OutOutput)
{
	// No output if inactive or zero weight
	if (CurrentState == EModifierState::Inactive || CurrentWeight <= KINDA_SMALL_NUMBER)
	{
		return false;
	}
	
	// Reset output
	OutOutput.Reset();
	
	// Fill transform outputs (apply weight)
	OutOutput.PositionOffset = PositionEffect * CurrentWeight;
	OutOutput.RotationOffset = RotationEffect * CurrentWeight;
	OutOutput.DistanceOffset = DistanceEffect * CurrentWeight;
	OutOutput.FOVOffset = FOVEffect * CurrentWeight;
	
	// Set flags based on what has values
	OutOutput. bHasPositionOffset = !PositionEffect.IsNearlyZero();
	OutOutput.bHasRotationOffset = !RotationEffect.IsNearlyZero();
	OutOutput.bHasDistanceOffset = !FMath::IsNearlyZero(DistanceEffect);
	OutOutput. bHasFOVOffset = !FMath:: IsNearlyZero(FOVEffect);
	
	// Fill post-process outputs
	OutOutput.VignetteIntensity = VignetteEffect * CurrentWeight;
	OutOutput.ColorTint = FMath:: Lerp(FLinearColor::White, ColorTintEffect, CurrentWeight);
	OutOutput. Saturation = FMath::Lerp(1.0f, SaturationEffect, CurrentWeight);
	OutOutput. bHasPostProcess = (VignetteEffect > KINDA_SMALL_NUMBER) || 
	                            (ColorTintEffect != FLinearColor:: White) ||
	                            (! FMath::IsNearlyEqual(SaturationEffect, 1.0f));
	
	// Fill time dilation (special handling - lerp towards effect value)
	OutOutput.TimeDilation = FMath:: Lerp(1.0f, TimeDilationEffect, CurrentWeight);
	OutOutput.bHasTimeDilation = ! FMath::IsNearlyEqual(TimeDilationEffect, 1.0f);
	
	// Blending info
	OutOutput. Weight = CurrentWeight;
	OutOutput.Priority = GetPriority();
	
	return true;
}


//========================================
// Virtual Callbacks (Default Implementations)
//========================================

void UCameraModifierBase::OnTriggered(const FModifierTriggerData& InTriggerData)
{
	// Default implementation - override in derived classes for custom setup
}

void UCameraModifierBase:: OnActivated()
{
	// Default implementation - called when fully blended in
}

void UCameraModifierBase::OnDeactivating()
{
	// Default implementation - called when blend out begins
}

void UCameraModifierBase:: OnDeactivated()
{
	// Default implementation - called when fully inactive
	// Reset effect values for next activation
	ResetEffectValues();
}

void UCameraModifierBase::ComputeEffect(float DeltaTime, const FStageExecutionContext& Context)
{
	// Default implementation - MUST override in derived classes
	// Derived classes should set: 
	// - PositionEffect
	// - RotationEffect
	// - DistanceEffect
	// - FOVEffect
	// - VignetteEffect
	// - ColorTintEffect
	// - SaturationEffect
	// - TimeDilationEffect
	//
	// Note: Weight is applied automatically in GetOutput()
}


//========================================
// Helper Methods
//========================================

/**
 * CalculateBlendWeight - Calculate the current blend weight based on state and timing
 * 
 * BlendIn:   0 → 1 over BlendInTime
 * Active:  1
 * BlendOut: 1 → 0 over BlendOutTime
 * Inactive: 0
 * 
 * @return Current blend weight (0.0 to 1.0)
 */
float UCameraModifierBase:: CalculateBlendWeight() const
{
	switch (CurrentState)
	{
		case EModifierState::BlendIn:
		{
			float BlendInTime = GetBlendInTime();
			if (BlendInTime <= 0.0f)
			{
				return 1.0f;
			}
			// Linear blend from 0 to 1
			return FMath::Clamp(StateTime / BlendInTime, 0.0f, 1.0f);
		}
		
		case EModifierState:: Active:
		{
			return 1.0f;
		}
		
		case EModifierState::BlendOut:
		{
			float BlendOutTime = GetBlendOutTime();
			if (BlendOutTime <= 0.0f)
			{
				return 0.0f;
			}
			// Linear blend from 1 to 0
			return FMath:: Clamp(1.0f - (StateTime / BlendOutTime), 0.0f, 1.0f);
		}
		
		case EModifierState:: Inactive: 
		default: 
		{
			return 0.0f;
		}
	}
}

/**
 * GetTimeRemaining - Get time remaining until modifier is fully inactive
 * 
 * @return Total remaining time in seconds
 */
float UCameraModifierBase::GetTimeRemaining() const
{
	switch (CurrentState)
	{
		case EModifierState:: BlendIn: 
		{
			// Remaining BlendIn + Active duration + BlendOut
			float RemainingBlendIn = FMath::Max(0.0f, GetBlendInTime() - StateTime);
			return RemainingBlendIn + ActiveDuration + GetBlendOutTime();
		}
		
		case EModifierState:: Active:
		{
			// Remaining Active + BlendOut
			float RemainingActive = FMath:: Max(0.0f, ActiveDuration - StateTime);
			return RemainingActive + GetBlendOutTime();
		}
		
		case EModifierState::BlendOut:
		{
			// Remaining BlendOut
			return FMath::Max(0.0f, GetBlendOutTime() - StateTime);
		}
		
		case EModifierState::Inactive:
		default: 
		{
			return 0.0f;
		}
	}
}

/**
 * TransitionToState - Change to a new state
 * 
 * Resets StateTime and updates CurrentState. 
 * 
 * @param NewState The new state to transition to
 */
void UCameraModifierBase::TransitionToState(EModifierState NewState)
{
	// Skip if already in this state
	if (CurrentState == NewState)
	{
		return;
	}
	
	// Update state
	CurrentState = NewState;
	StateTime = 0.0f;
}

/**
 * CreateBaseOutput - Create an output structure with current settings
 * 
 * @return FModifierOutput with weight and priority set
 */
FModifierOutput UCameraModifierBase::CreateBaseOutput() const
{
	FModifierOutput Output;
	Output.Weight = CurrentWeight;
	Output. Priority = GetPriority();
	return Output;
}

/**
 * ResetEffectValues - Reset all effect values to defaults
 * 
 * Called at the start of Trigger() and end of OnDeactivated()
 */
void UCameraModifierBase::ResetEffectValues()
{
	PositionEffect = FVector:: ZeroVector;
	RotationEffect = FRotator::ZeroRotator;
	DistanceEffect = 0.0f;
	FOVEffect = 0.0f;
	VignetteEffect = 0.0f;
	ColorTintEffect = FLinearColor::White;
	SaturationEffect = 1.0f;
	TimeDilationEffect = 1.0f;
}
```

---

【代码风格要求】

1. **注释：**
   - 文件头注释说明基类职责
   - 每个重要方法都有文档注释
   - 解释状态转换逻辑
   - 所有注释英文

2. **格式：**
   - Tab缩进
   - 清晰的区块分隔
   - 方法间空两行

3. **防御性编程：**
   - 无效状态检查
   - 权重边界检查
   - 时间值边界检查

【技术说明】

### 1. 状态机逻辑

```
Update() 每帧调用：

BlendIn状态:
├─ StateTime += DeltaTime
├─ if (StateTime >= BlendInTime)
│   └─ TransitionToState(Active)
└─ CurrentWeight = StateTime / BlendInTime

Active状态:
├─ StateTime += DeltaTime
├─ if (StateTime >= ActiveDuration && ActiveDuration > 0)
│   └─ TransitionToState(BlendOut)
└─ CurrentWeight = 1.0

BlendOut状态:
├─ StateTime += DeltaTime
├─ if (StateTime >= BlendOutTime)
│   └─ TransitionToState(Inactive)
└─ CurrentWeight = 1.0 - (StateTime / BlendOutTime)
```

### 2. 权重应用

```cpp
// ComputeEffect设置原始效果值（不含权重）
PositionEffect = FVector(10.0f, 0.0f, 0.0f);

// GetOutput应用权重
OutOutput.PositionOffset = PositionEffect * CurrentWeight;
// 例：权重0.5时，输出(5, 0, 0)
```

### 3. 后处理效果混合

```cpp
// 颜色使用Lerp混合
OutOutput.ColorTint = FMath:: Lerp(FLinearColor::White, ColorTintEffect, CurrentWeight);
// 权重0.5时，颜色在White和目标色之间

// 饱和度同理
OutOutput. Saturation = FMath::Lerp(1.0f, SaturationEffect, CurrentWeight);
// 权重0.5时，饱和度在1.0和目标值之间
```

### 4. 时间膨胀处理

```cpp
// 时间膨胀需要特殊处理
// 因为1.0是正常速度，需要平滑过渡
OutOutput.TimeDilation = FMath:: Lerp(1.0f, TimeDilationEffect, CurrentWeight);
// TimeDilationEffect=0.5, Weight=0.5 → 输出0.75（中间速度）
```

### 5. ResetEffectValues的意义

```cpp
void ResetEffectValues()
{
    // 确保每次Trigger时从干净状态开始
    // 确保Inactive后没有残留效果
    
    PositionEffect = FVector::ZeroVector;
    // ...  所有效果重置
}
```

【验证标准】

1. **编译验证：**
   - [ ] Regenerate执行
   - [ ] 编译通过（0 errors）
   - [ ] UTF-8 Without BOM
   - [ ] 注释英文

2. **代码完整性：**
   - [ ] 构造函数初始化所有成员
   - [ ] Trigger()完整实现
   - [ ] Stop()完整实现
   - [ ] Update()状态机完整
   - [ ] GetOutput()权重应用正确
   - [ ] 5个虚拟回调有默认实现
   - [ ] 4个辅助方法完整

3. **逻辑正确性：**
   - [ ] 状态转换正确触发回调
   - [ ] 权重计算符合生命周期
   - [ ] 后处理效果使用Lerp混合
   - [ ] 时间膨胀平滑过渡
   - [ ] ActiveDuration=0表示无限

4. **与. h文件一致性：**
   - [ ] 所有声明的方法都有实现
   - [ ] 成员变量初始化顺序与声明顺序一致
   - [ ] EModifierState枚举值名称匹配

【注意事项】

1. **枚举值名称：**
   ```cpp
   // 正确
   EModifierState::BlendIn
   EModifierState::BlendOut
   
   // 错误（原始PROMPT使用的）
   EModifierState::BlendingIn
   EModifierState::BlendingOut
   ```

2. **新增方法：**
   原始PROMPT没有的，修改版添加的：
   ```cpp
   void ResetEffectValues();  // 需要在. h中添加声明
   ```
   
   **如果. h中没有声明，需要添加：**
   ```cpp
   protected:
       /** Reset all effect values to defaults */
       void ResetEffectValues();
   ```

3. **UE_LOG移除：**
   原始PROMPT有大量UE_LOG，修改版移除以提高性能。
   如需调试，可自行添加。

4. **KINDA_SMALL_NUMBER：**
   使用UE4内置常量进行浮点数比较。

【输出要求】

```
================================================================================
Sub-Step 4.1.2 完成报告
================================================================================

【创建的文件】
- 文件：CameraModifierBase. cpp
- 路径：Source/[ProjectName]/Private/Camera/Modifiers/
- 代码行数：约XXX行

【实现的功能】
- ✅ 构造函数（初始化所有成员）
- ✅ GetModifierName/Description
- ✅ Trigger（激活修改器）
- ✅ Stop（停止修改器）
- ✅ Update（状态机更新）
- ✅ GetOutput（获取输出）
- ✅ 5个虚拟回调（OnTriggered等）
- ✅ 4个辅助方法（CalculateBlendWeight等）

【编译结果】
- Regenerate：[已执行]
- 编译状态：[成功/失败]
- 错误数：0
- 警告数：[数量]

【需要修改. h文件】
- ResetEffectValues()声明：[已添加/不需要]

【最终状态】
✅ CameraModifierBase基类实现完成

================================================================================
```

【后续步骤】

CameraModifierBase完成后：
- [x] ✅ 4. 1.1: CameraModifierBase.h
- [x] ✅ 4.1.2: CameraModifierBase.cpp
- [ ] ⏳ 4.1.3: CameraModifierManager.h（下一步）
- [ ] ⏳ 4.1.4: CameraModifierManager. cpp
- [ ] ⏳ 4.2+: 具体修改器实现

**Phase 4基础设施进度：2/4文件 = 50%**

================================================================================