Sub-Step 3.4.8 Prompt

================================================================================
任务：在CameraModule_Distance. cpp中添加D07 Boss_Phase实现
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
D07为Boss战不同阶段提供距离缩放乘数。
这是Distance模块系列的最后一个模块，完成后Step 3.4全部完成。

【Pipeline中的位置】
```
Distance模块计算流程（完整链）：
1. D01: BaseDistance = 400cm (Override)
2. D02: ×1.3 (Multiplicative，大型目标) → 520cm
3. D03: +30cm (Additive，移动中) → 550cm
4. D04: -50cm (Additive，战斗) → 500cm
5. D05: min(500, 无限) (Minimum) → 500cm
6. D06: Blend(500, 450, 0.6) → 480cm
7. D07: ×1.2 (Multiplicative，Boss阶段2) ← 当前
   → 480 × 1.2 = 576cm（Boss战最终距离）
```

【当前进度】
Phase 3 Step 3.4 Sub-Step 3.4.8 (共190个Sub-Step中的第86个)
前置完成：3.4.7 D06实现 ✅
**这是Step 3.4的最后一个Sub-Step**

【任务目标】
在CameraModule_Distance.cpp中完整实现D07模块，替换现有的占位符。
完成后，所有7个Distance模块（D01-D07）全部实现完毕。

【⚠️ 重要提醒】

1. **编译前必做步骤：**
   本Sub-Step只修改现有. cpp文件，不添加新文件。
   因此**不需要Regenerate**，可以直接编译。

2. **编码格式要求：**
   - 文件编码：UTF-8 Without BOM
   - 所有注释：英文

3. **命名替换：**
   确认StateConfig和InputContext的字段路径与项目实际结构一致。

【具体要求】

1. **文件路径：** Source/[ProjectName]/Private/Camera/Modules/CameraModule_Distance. cpp

2. **定位修改位置：**

在文件中找到D07的占位符实现部分：

```cpp
//========================================
// D07: Boss Phase
//========================================

// TODO: Full implementation in Sub-Step 3.4.8
// ...   占位符代码
```

**将上述占位符代码完整替换为下方的完整实现。**

---

3. **D07完整实现代码：**

**A. ShouldActivate() 实现：**

**当前占位符代码：**
```cpp
bool UCameraModule_D07_Boss_Phase::ShouldActivate(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig) const
{
	// Placeholder:   Will be properly implemented in Sub-Step 3.4.8
	return false;
}
```

**替换为完整实现：**
```cpp
/**
 * ShouldActivate - Determine if boss phase distance scaling should be active
 * 
 * D07 activates when:  
 * - Module is enabled
 * - Player has a locked target
 * - Target IS a boss (bTargetIsBoss)
 * 
 * This module provides phase-specific distance scaling for boss fights.
 * Different boss phases may require different camera distances for
 * optimal gameplay visibility and cinematic effect.  
 * 
 * Priority 125 (highest among Distance modules), uses Multiplicative policy.  
 * This ensures boss-specific scaling is applied last, on top of all other
 * distance calculations.
 * 
 * Use Cases:
 * - Phase 1: Normal distance (1. 0x)
 * - Phase 2 (Enraged): Pull back slightly (1.2x)
 * - Phase 3 (Final): Maximum pullback for dramatic effect (1.4x)
 * - Large bosses:  Additional size-based scaling
 */
bool UCameraModule_D07_Boss_Phase::ShouldActivate(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig) const
{
	// Check if module is enabled
	if (!bIsEnabled)
	{
		return false;
	}
	
	// Check if player has a locked target
	if (!Context.InputContext.bHasTarget)
	{
		return false;
	}
	
	// Check if target IS a boss
	// This is the key condition that distinguishes D07 from D02
	if (!Context. InputContext.bTargetIsBoss)
	{
		return false;
	}
	
	// All conditions met - activate boss phase scaling
	return true;
}
```

**说明：**
- 三个条件：启用 + 有目标 + 目标是Boss
- 与D02区别：D02处理所有大型目标，D07仅Boss
- 优先级125（Distance模块最高）
- Multiplicative策略

---

**B. Compute() 实现：**

**当前占位符代码：**
```cpp
bool UCameraModule_D07_Boss_Phase::Compute(...)
{
	// Placeholder
	OutOutput = CreateBaseOutput();
	OutOutput.DistanceOutput = 1.0f;
	OutOutput. bHasDistanceOutput = true;
	OutOutput.bDistanceIsMultiplier = true;
	return true;
}
```

**替换为完整实现：**
```cpp
/**
 * Compute - Calculate boss phase distance multiplier
 * 
 * This module calculates a distance multiplier specifically for boss fights.
 * It considers both the boss phase (from config) and the boss size to
 * determine the optimal camera distance. 
 * 
 * Steps:
 * 1. Validate boss target
 * 2. Get boss phase distance scale from config
 * 3. Calculate size-based modifier for large bosses
 * 4. Combine phase scale with size modifier
 * 5. Clamp to valid range
 * 6. Output with Multiplicative policy
 * 
 * Phase Scaling Examples:
 * - Phase 1 (Normal):  BossPhaseDistanceScale = 1.0
 * - Phase 2 (Enraged): BossPhaseDistanceScale = 1.2
 * - Phase 3 (Final):   BossPhaseDistanceScale = 1.4
 * 
 * Size Modifier (for very large bosses):
 * - Boss < 300cm:   modifier = 1.0 (no additional scaling)
 * - Boss 300-800cm: modifier = 1.0 to 1.3 (gradual increase)
 * - Boss > 800cm:  modifier = 1.3 (capped)
 * 
 * Final Multiplier = PhaseScale × SizeModifier
 * Clamped to 0.8 - 2.0 range
 * 
 * @param Context The current pipeline execution context
 * @param StateConfig The current camera state configuration
 * @param OutOutput Output structure to fill with computed values
 * @return True if computation succeeded
 */
bool UCameraModule_D07_Boss_Phase:: Compute(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig, 
	FModuleOutput& OutOutput)
{
	// Create base output with module identity
	OutOutput = CreateBaseOutput();
	
	// Step 1: Validate boss target
	if (!Context.InputContext.bTargetIsBoss)
	{
		// Not a boss - return multiplier of 1.0 (no change)
		OutOutput.DistanceOutput = 1.0f;
		OutOutput.bHasDistanceOutput = true;
		OutOutput.bDistanceIsMultiplier = true;
		return false;
	}
	
	// Step 2: Get boss phase distance scale from config
	// This value is typically set by the boss fight system based on current phase
	float BossPhaseScale = StateConfig.Module. Distance. BossPhaseDistanceScale;
	
	// If not configured, use default (Phase 1 = no scaling)
	if (BossPhaseScale <= 0.0f)
	{
		BossPhaseScale = 1.0f;  // Default:  no phase scaling
	}
	
	// Step 3: Calculate size-based modifier for large bosses
	// Very large bosses may need additional distance beyond phase scaling
	float TargetSize = Context.InputContext. TargetSize;
	float SizeModifier = 1.0f;
	
	// Size threshold for additional scaling
	const float LargeBossThreshold = 300.0f;  // 3 meters
	const float MaxSizeForScaling = 800.0f;   // 8 meters (cap)
	const float MaxSizeModifier = 1.3f;       // Max 30% additional
	
	if (TargetSize > LargeBossThreshold)
	{
		// Calculate how much larger than threshold
		float SizeAboveThreshold = TargetSize - LargeBossThreshold;
		float ScalingRange = MaxSizeForScaling - LargeBossThreshold;  // 500cm
		
		// Calculate modifier (0 to 0.3, then add 1.0)
		// At 300cm: modifier = 1.0
		// At 550cm: modifier = 1.15
		// At 800cm+: modifier = 1.3 (capped)
		float ModifierAdd = (SizeAboveThreshold / ScalingRange) * (MaxSizeModifier - 1.0f);
		SizeModifier = 1.0f + FMath::Clamp(ModifierAdd, 0.0f, MaxSizeModifier - 1.0f);
	}
	
	// Step 4: Combine phase scale with size modifier
	// Both are multipliers, so they compound
	float FinalMultiplier = BossPhaseScale * SizeModifier;
	
	// Step 5: Clamp to reasonable range
	// Minimum 0.8: Prevent camera from getting too close to boss
	// Maximum 2.0: Prevent camera from getting too far (match D02 limit)
	FinalMultiplier = FMath::Clamp(FinalMultiplier, 0.8f, 2.0f);
	
	// Step 6: Fill output structure
	OutOutput. DistanceOutput = FinalMultiplier;
	OutOutput.bHasDistanceOutput = true;
	OutOutput. bDistanceIsMultiplier = true;  // This is a multiplier, not absolute
	
	// Full weight for boss fights - authoritative scaling
	OutOutput. Weight = 1.0f;
	
	return true;
}
```

**说明：**
- 阶段缩放 × 尺寸修正 = 最终乘数
- 阶段缩放由游戏系统设置
- 大型Boss额外增加距离
- 乘数范围0.8-2.0
- 满权重（权威缩放）

---

4. **完整的D07代码块（用于复制粘贴）：**

```cpp
//========================================
// D07: Boss Phase
//========================================

/**
 * ShouldActivate - Activate only for boss targets
 */
bool UCameraModule_D07_Boss_Phase::ShouldActivate(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig) const
{
	// Check if module is enabled
	if (!bIsEnabled)
	{
		return false;
	}
	
	// Check if player has a locked target
	if (!Context.InputContext.bHasTarget)
	{
		return false;
	}
	
	// Check if target IS a boss
	if (!Context.InputContext.bTargetIsBoss)
	{
		return false;
	}
	
	return true;
}

/**
 * Compute - Calculate boss phase distance multiplier
 */
bool UCameraModule_D07_Boss_Phase::Compute(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig, 
	FModuleOutput& OutOutput)
{
	// Create base output
	OutOutput = CreateBaseOutput();
	
	// Validate boss target
	if (!Context.InputContext.bTargetIsBoss)
	{
		OutOutput.DistanceOutput = 1.0f;
		OutOutput.bHasDistanceOutput = true;
		OutOutput.bDistanceIsMultiplier = true;
		return false;
	}
	
	// Get boss phase distance scale
	float BossPhaseScale = StateConfig.Module.Distance. BossPhaseDistanceScale;
	if (BossPhaseScale <= 0.0f)
	{
		BossPhaseScale = 1.0f;  // Default: no scaling
	}
	
	// Calculate size-based modifier for large bosses
	float TargetSize = Context.InputContext. TargetSize;
	float SizeModifier = 1.0f;
	
	const float LargeBossThreshold = 300.0f;
	const float MaxSizeForScaling = 800.0f;
	const float MaxSizeModifier = 1.3f;
	
	if (TargetSize > LargeBossThreshold)
	{
		float SizeAboveThreshold = TargetSize - LargeBossThreshold;
		float ScalingRange = MaxSizeForScaling - LargeBossThreshold;
		float ModifierAdd = (SizeAboveThreshold / ScalingRange) * (MaxSizeModifier - 1.0f);
		SizeModifier = 1.0f + FMath::Clamp(ModifierAdd, 0.0f, MaxSizeModifier - 1.0f);
	}
	
	// Combine phase scale with size modifier
	float FinalMultiplier = BossPhaseScale * SizeModifier;
	
	// Clamp to valid range
	FinalMultiplier = FMath::Clamp(FinalMultiplier, 0.8f, 2.0f);
	
	// Fill output
	OutOutput.DistanceOutput = FinalMultiplier;
	OutOutput.bHasDistanceOutput = true;
	OutOutput.bDistanceIsMultiplier = true;
	OutOutput.Weight = 1.0f;
	
	return true;
}
```

---

【代码风格要求】

1. **注释：**
   - 说明阶段缩放的概念
   - 解释尺寸修正的计算
   - 提供阶段示例
   - 所有注释英文

2. **格式：**
   - Tab缩进
   - 清晰步骤标注
   - 方法间空两行

3. **防御性编程：**
   - 验证Boss目标
   - 配置值默认值处理
   - Clamp确保有效范围

【技术说明】

**D07的核心机制：**

### 1. 阶段缩放概念

**Boss战阶段系统：**

```
典型Boss战阶段设计：

Phase 1 - 正常阶段：
- BossPhaseDistanceScale = 1.0
- 相机距离：正常
- 玩家学习Boss模式

Phase 2 - 狂暴阶段：
- BossPhaseDistanceScale = 1.2
- 相机距离：拉远20%
- Boss动作更激烈，需要更大视野

Phase 3 - 最终阶段：
- BossPhaseDistanceScale = 1.4
- 相机距离：拉远40%
- 戏剧性高潮，史诗视角

实现：
- 游戏系统设置BossPhaseDistanceScale
- D07读取并应用
- 阶段转换时数值变化
- 相机平滑过渡（由Stage 5处理）
```

### 2. 尺寸修正的作用

**为什么需要额外的尺寸修正？**

```
问题：
- D02已经处理目标尺寸
- 为什么D07还要考虑尺寸？

答案：
- D02是通用的，处理所有大型敌人
- D07是Boss特定的，叠加在D02之上
- 某些超大Boss需要更多空间

示例：
普通大型敌人（300cm）：
- D02: ×1.3（尺寸乘数）
- D07: 不激活（不是Boss）

大型Boss（500cm）：
- D02: ×1.5（尺寸乘数）
- D07: ×1.0（Phase 1）× 1.2（尺寸修正）= ×1.2
- 总效果：×1.5 × 1.2 = ×1.8

巨型Boss（800cm），Phase 2：
- D02: ×2.0（capped）
- D07: ×1.2（Phase 2）× 1.3（尺寸修正）= ×1.56 → capped到×2.0
- 总效果：看后续计算
```

### 3. D02 vs D07 的分工

| 特性 | D02 (TargetSize) | D07 (BossPhase) |
|------|------------------|-----------------|
| **触发条件** | 任何大型目标 | 仅Boss |
| **基于** | 目标物理尺寸 | 战斗阶段+尺寸 |
| **优先级** | 110 | 125 |
| **主要目的** | 基础尺寸适应 | 阶段性调整 |
| **可配置性** | 固定公式 | 动态阶段值 |

**叠加效果：**
```
场景：锁定500cm的Boss，Phase 2

D02 (110): ×1.5（尺寸乘数）
D07 (125): ×1.2（Phase 2）× 1.12（尺寸修正）= ×1.344

Stage 5 Multiplicative组合：
1. 5 × 1.344 = 2.016 → capped到2.0

最终效果：基础距离 × 2.0
```

### 4. 乘数范围设计

**为什么范围是0.8-2.0？**

```
最小值0.8：
- 允许Phase设计者创建"拉近"效果
- 例如：BossPhaseScale = 0.8（特写阶段）
- 防止太近导致视角问题

最大值2.0：
- 与D02保持一致
- 防止相机过远
- 最大距离 = BaseDistance × 2.0 = 800cm

实际应用：
- Phase 1: 1.0（正常）
- Phase 2: 1.2（稍远）
- Phase 3: 1.4（更远）
- Special: 0.9（特殊近距离阶段）
```

### 5. 完整计算示例

**场景：巨龙Boss，Phase 2**

```
配置：
- BaseDistance = 400cm
- Boss Size = 600cm（巨龙）
- BossPhaseDistanceScale = 1.2（Phase 2）

计算链：
1. D01: 400cm (Override)
2. D02: ×1.5 → 600cm（大型目标）
3. D03: +0cm → 600cm（不移动）
4. D04: -50cm → 550cm（战斗调整）
5. D05: min(550, ∞) → 550cm（无环境限制）
6. D06: Blend → ~520cm（接近调整）
7. D07: ×1.2 × 1.18 = ×1.416 → 520 × 1.416 = 736cm

最终相机距离：736cm

对比普通敌人（无Boss）：
1-6步结果：~450cm
7步不激活
最终：450cm

Boss战的相机明显更远（736 vs 450）
```

【验证标准】

1. **编译验证：**
   - [ ] 编译通过
   - [ ] UTF-8 Without BOM
   - [ ] 注释英文

2. **代码完整性：**
   - [ ] ShouldActivate()完整
   - [ ] Compute()完整
   - [ ] 阶段缩放+尺寸修正

3. **逻辑正确性：**
   - [ ] 检查bTargetIsBoss
   - [ ] 阶段缩放默认1.0
   - [ ] 尺寸修正计算正确
   - [ ] 两者相乘
   - [ ] Clamp范围0.8-2.0
   - [ ] bDistanceIsMultiplier = true
   - [ ] Weight = 1.0

4. **字段访问：**
   - [ ] Context.InputContext.bHasTarget
   - [ ] Context.InputContext.bTargetIsBoss
   - [ ] Context. InputContext. TargetSize
   - [ ] StateConfig.Module.Distance.BossPhaseDistanceScale

【注意事项】

1. **不需要Regenerate**

2. **字段缺失处理：**
   ```cpp
   // 如果BossPhaseDistanceScale不存在
   float BossPhaseScale = 1.0f;  // 直接使用默认值
   ```

3. **阶段值设置：**
   Boss战系统应该动态设置BossPhaseDistanceScale：
   ```cpp
   // 在Boss战系统中
   void OnBossPhaseChange(int32 NewPhase)
   {
       switch(NewPhase)
       {
           case 1:  CameraStateConfig.Module.Distance.BossPhaseDistanceScale = 1.0f; break;
           case 2: CameraStateConfig.Module. Distance.BossPhaseDistanceScale = 1.2f; break;
           case 3: CameraStateConfig.Module.Distance.BossPhaseDistanceScale = 1.4f; break;
       }
   }
   ```

4. **测试建议：**
   - 锁定Boss → D07应激活
   - 不同阶段测试缩放效果
   - 不同尺寸Boss测试尺寸修正
   - 确认乘数在0.8-2.0范围内

【输出要求】

```
================================================================================
Sub-Step 3.4.8 完成报告
================================================================================

【修改内容】
- 文件：CameraModule_Distance. cpp
- 修改位置：D07模块
- 代码行数：约XX行

【实现的功能】
- ✅ ShouldActivate：检查Boss目标
- ✅ Compute：阶段缩放+尺寸修正

【编译结果】
- 编译状态：[成功/失败]

【最终状态】
✅ D07完成，Boss阶段距离缩放

================================================================================
```

【🎉 Step 3.4 完成庆祝】

**Distance模块系列全部完成！**

完成后，Distance模块最终状态：
- [x] ✅ D01:  Base Distance（基础距离，Override）
- [x] ✅ D02: Target Size Multiplier（目标尺寸乘数，Multiplicative）
- [x] ✅ D03: Speed Offset（速度偏移，Additive）
- [x] ✅ D04: Combat Adjust（战斗调整，Additive）
- [x] ✅ D05: Environment Limit（环境限制，Minimum）
- [x] ✅ D06: Proximity Adjust（接近调整，Blend）
- [x] ✅ D07: Boss Phase（Boss阶段，Multiplicative）

**Distance模块完成度：7/7 (100%)** 🎊

**模块分类总结：**

| 混合策略 | 模块 | 用途 |
|---------|------|------|
| **Override** | D01 | 基础值 |
| **Additive** | D03, D04 | 偏移调整 |
| **Multiplicative** | D02, D07 | 比例缩放 |
| **Minimum** | D05 | 上限约束 |
| **Blend** | D06 | 加权混合 |

**Phase 3 总进度：**
- [x] ✅ Step 3.1: 模块基础设施（4个文件）
- [x] ✅ Step 3.2: Position模块（8个模块）
- [x] ✅ Step 3.3: Rotation模块（9个模块）
- [x] ✅ Step 3.4: Distance模块（7个模块）
- [ ] ⏳ Step 3.5: FOV模块（6个模块）
- [ ] ⏳ Step 3.6: Offset模块（5个模块）
- [ ] ⏳ Step 3.7: Constraint模块（4个模块）

**已完成模块：24/39 (61.5%)**

**下一步：**
- Step 3.5: FOV模块（视野角度控制）
- 6个FOV模块：F01-F06

**准备好进入FOV模块了吗？** 🚀

完成D07后告诉我："Distance完成，准备FOV"

================================================================================