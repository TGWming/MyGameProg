Sub-Step 3.7.4 Prompt

================================================================================
任务：在CameraModule_Constraint.cpp中添加C03 FOV_Clamp实现
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
C03负责限制FOV（视野角度）在配置的范围内。
这确保视野不会过窄（隧道视觉）或过宽（鱼眼变形）。

【Pipeline中的位置】
```
Stage 3 - Module Compute (Constraint部分):
├─ C01:  Pitch Clamp (Override, 200) ✅ 已完成
├─ C02: Distance Clamp (Override, 200) ✅ 已完成
├─ C03: FOV Clamp (Override, 200) ← 当前
│   → 限制FOV在 MinFOV 到 MaxFOV 范围内
└─ C04: Visibility Ensure (Blend, 210)

FOV限制的重要性：
- 防止多个Additive FOV模块累加后过度极端
- 避免极窄FOV导致的隧道视觉感
- 避免极宽FOV导致的鱼眼变形
- 确保舒适的游戏体验
```

【当前进度】
Phase 3 Step 3.7 Sub-Step 3.7.4 (共190个Sub-Step中的第103个)
前置完成：3.7.3 C02实现 ✅

【任务目标】
在CameraModule_Constraint.cpp中完整实现C03模块，替换现有的占位符。

【⚠️ 重要提醒】

1. **编译前必做步骤：**
   本Sub-Step只修改现有.  cpp文件，不添加新文件。
   因此**不需要Regenerate**，可以直接编译。

2. **编码格式要求：**
   - 文件编码：UTF-8 Without BOM
   - 所有注释：英文

3. **命名替换：**
   确认StateConfig的字段路径与项目实际结构一致。

【具体要求】

1. **文件路径：** Source/[ProjectName]/Private/Camera/Modules/CameraModule_Constraint. cpp

2. **定位修改位置：**

在文件中找到C03的占位符实现部分：

```cpp
//========================================
// C03: FOV Clamp
//========================================

// TODO: Full implementation in Sub-Step 3.7.4
// Enforces field of view limits
...  占位符代码
```

**将上述占位符代码完整替换为下方的完整实现。**

---

3. **C03完整实现代码：**

**A. ShouldActivate() 实现：**

```cpp
/**
 * ShouldActivate - FOV clamp is always active
 * 
 * C03 is a safety constraint that should always be active.  
 * It ensures the camera FOV never exceeds configured limits,
 * regardless of what other modules have calculated.
 * 
 * Priority 200 (constraint level), uses Override policy. 
 */
bool UCameraModule_C03_FOV_Clamp::ShouldActivate(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig) const
{
	// FOV clamp is always active as a safety constraint
	// Only check if module is enabled
	return bIsEnabled;
}
```

---

**B. Compute() 实现：**

```cpp
/**
 * Compute - Clamp camera FOV to configured limits
 * 
 * This module reads the current accumulated FOV from the context
 * and clamps it to the configured min/max values.
 * 
 * Steps:
 * 1. Get current FOV from context output
 * 2. Get FOV limits from state config
 * 3. Apply default limits if not configured
 * 4. Validate min/max relationship
 * 5. Clamp FOV to valid range
 * 6. Output the clamped FOV
 * 
 * FOV Units:  Degrees
 * 
 * Typical Limits:
 * - MinFOV: 50-60 degrees (prevent tunnel vision)
 * - MaxFOV: 120-130 degrees (prevent fisheye distortion)
 * 
 * FOV Effects on Player:
 * - Too narrow (< 60°): Uncomfortable tunnel vision, disorienting
 * - Too wide (> 120°): Fisheye distortion, motion sickness
 * - Sweet spot: 80-100° for third-person games
 * 
 * Why Constraint is Important:
 * - Multiple Additive FOV modules can stack (Speed + Combat + Boss)
 * - Without limits, FOV could reach extreme values
 * - Example: Base(90) + Speed(+15) + Combat(+5) + Boss(+15) = 125°
 * 
 * @param Context The current pipeline execution context
 * @param StateConfig The current camera state configuration
 * @param OutOutput Output structure to fill with computed values
 * @return True if computation succeeded
 */
bool UCameraModule_C03_FOV_Clamp::Compute(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig, 
	FModuleOutput& OutOutput)
{
	// Create base output with module identity
	OutOutput = CreateBaseOutput();
	
	// Step 1: Get current FOV from context output
	// This is the accumulated FOV from all previous modules
	float CurrentFOV = Context.Output.FOV;
	
	// Step 2: Get FOV limits from state config
	float MinFOV = StateConfig. StateBase.FOV.MinFOV;
	float MaxFOV = StateConfig.StateBase. FOV.MaxFOV;
	
	// Step 3: Apply default limits if not configured
	// MinFOV should be reasonable (not too narrow)
	if (MinFOV <= 0.0f || MinFOV < 30.0f)
	{
		MinFOV = 60.0f;  // Default: 60 degrees minimum
	}
	
	// MaxFOV should be reasonable (not too wide)
	if (MaxFOV <= 0.0f || MaxFOV > 150.0f)
	{
		MaxFOV = 120.0f;  // Default: 120 degrees maximum
	}
	
	// Step 4: Validate min/max relationship
	// Ensure min < max (swap if inverted)
	if (MinFOV > MaxFOV)
	{
		float Temp = MinFOV;
		MinFOV = MaxFOV;
		MaxFOV = Temp;
	}
	
	// Also ensure there's at least some valid range
	if (MaxFOV - MinFOV < 10.0f)
	{
		// If range is too small, expand it symmetrically
		float Center = (MinFOV + MaxFOV) / 2.0f;
		MinFOV = Center - 15.0f;
		MaxFOV = Center + 15.0f;
	}
	
	// Step 5: Clamp FOV to valid range
	float ClampedFOV = FMath::Clamp(CurrentFOV, MinFOV, MaxFOV);
	
	// Step 6: Fill output structure
	OutOutput.FOVOutput = ClampedFOV;
	OutOutput.bHasFOVOutput = true;
	
	return true;
}
```

---

4. **完整的C03代码块（用于复制粘贴）：**

```cpp
//========================================
// C03: FOV Clamp - Enforces field of view limits
//========================================

/**
 * ShouldActivate - FOV clamp is always active
 */
bool UCameraModule_C03_FOV_Clamp:: ShouldActivate(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig) const
{
	// FOV clamp is always active as a safety constraint
	return bIsEnabled;
}

/**
 * Compute - Clamp camera FOV to configured limits
 */
bool UCameraModule_C03_FOV_Clamp::Compute(
	const FStageExecutionContext& Context, 
	const FCameraStateConfig& StateConfig, 
	FModuleOutput& OutOutput)
{
	// Create base output
	OutOutput = CreateBaseOutput();
	
	// Get current FOV from context output
	float CurrentFOV = Context.Output. FOV;
	
	// Get FOV limits from state config
	float MinFOV = StateConfig.StateBase.FOV. MinFOV;
	float MaxFOV = StateConfig. StateBase.FOV.MaxFOV;
	
	// Apply default limits if not configured
	if (MinFOV <= 0.0f || MinFOV < 30.0f)
	{
		MinFOV = 60.0f;  // Default: 60 degrees minimum
	}
	
	if (MaxFOV <= 0.0f || MaxFOV > 150.0f)
	{
		MaxFOV = 120.0f;  // Default: 120 degrees maximum
	}
	
	// Validate min/max relationship
	if (MinFOV > MaxFOV)
	{
		float Temp = MinFOV;
		MinFOV = MaxFOV;
		MaxFOV = Temp;
	}
	
	// Ensure minimum valid range
	if (MaxFOV - MinFOV < 10.0f)
	{
		float Center = (MinFOV + MaxFOV) / 2.0f;
		MinFOV = Center - 15.0f;
		MaxFOV = Center + 15.0f;
	}
	
	// Clamp FOV
	float ClampedFOV = FMath::Clamp(CurrentFOV, MinFOV, MaxFOV);
	
	// Fill output
	OutOutput.FOVOutput = ClampedFOV;
	OutOutput.bHasFOVOutput = true;
	
	return true;
}
```

---

【代码风格要求】

1. **注释：**
   - 说明FOV单位（度）
   - 解释典型限制值
   - 描述FOV对玩家体验的影响
   - 说明Additive累加风险
   - 所有注释英文

2. **格式：**
   - Tab缩进
   - 清晰步骤标注
   - 方法间空两行

3. **防御性编程：**
   - 默认值处理
   - Min/Max验证和交换
   - 最小有效范围保证（对称扩展）

【技术说明】

**C03的核心机制：**

### 1. FOV累加问题

```
FOV模块的累加情况：

F01 BaseFOV: 90°
F02 SpeedFOV: +15° (冲刺中)
F04 CombatFOV: +5° (战斗中)
F05 BossFOV: +15° (Boss战)
────────────────────
累计: 125°

问题：125°会产生明显的鱼眼变形
解决：C03限制最大120° → 输出120°
```

### 2. FOV对体验的影响

```
FOV范围和感受：

< 60°  → 隧道视觉，压迫感，不适
60-80° → 略窄，聚焦感，适合瞄准
80-100° → 标准范围，舒适
100-120° → 略宽，速度感，周边视野好
> 120° → 鱼眼变形，可能导致晕眩

第三人称游戏推荐：80-100°基础值
```

### 3. 与F01 BaseFOV的关系

```
F01 BaseFOV: 
- 位于FOV模块最前面
- 提供配置的基础FOV值
- 也有Min/Max限制

C03 FOV Clamp: 
- 位于所有模块最后面
- 限制所有模块累加后的结果
- 是最终的安全网

区别：
- F01限制的是"基础配置值"
- C03限制的是"最终累加值"
```

### 4. 默认值的选择

```cpp
// MinFOV = 60°
// 原因：
// - 低于60°会有明显的隧道视觉
// - 即使瞄准状态也不应低于60°
// - 30°是绝对下限（异常配置保护）

// MaxFOV = 120°
// 原因：
// - 超过120°会有明显的鱼眼变形
// - 可能导致玩家晕眩
// - 150°是绝对上限（异常配置保护）
```

### 5. 范围扩展策略

```cpp
// 如果配置的范围太小（< 10°）
if (MaxFOV - MinFOV < 10.0f)
{
    // 对称扩展
    float Center = (MinFOV + MaxFOV) / 2.0f;
    MinFOV = Center - 15.0f;
    MaxFOV = Center + 15.0f;
}

// 示例：
// 配置 MinFOV=88, MaxFOV=92 (范围4°)
// Center = 90
// 扩展后 MinFOV=75, MaxFOV=105 (范围30°)

// 原因：范围太小会导致所有FOV调整失效
```

【验证标准】

1. **编译验证：**
   - [ ] 编译通过
   - [ ] UTF-8 Without BOM
   - [ ] 注释英文

2. **代码完整性：**
   - [ ] ShouldActivate()完整
   - [ ] Compute()完整
   - [ ] 所有方法签名与. h一致

3. **逻辑正确性：**
   - [ ] 从Context. Output. FOV读取
   - [ ] 默认MinFOV = 60°
   - [ ] 默认MaxFOV = 120°
   - [ ] Min/Max验证和交换
   - [ ] 最小范围保证（对称扩展）

4. **字段访问：**
   - [ ] Context.Output.FOV
   - [ ] StateConfig.StateBase. FOV. MinFOV
   - [ ] StateConfig.StateBase.FOV.MaxFOV

【注意事项】

1. **不需要Regenerate**

2. **字段缺失处理：**
   ```cpp
   // 如果MinFOV/MaxFOV字段不存在
   float MinFOV = 60.0f;   // 默认值
   float MaxFOV = 120.0f;  // 默认值
   ```

3. **UE_LOG已移除：**
   原始PROMPT中的UE_LOG已移除，减少运行时日志。如需调试，可自行添加。

4. **测试建议：**
   - 设置FOV = 40 → 应被限制为60（或MinFOV）
   - 设置FOV = 140 → 应被限制为120（或MaxFOV）
   - 设置FOV = 90 → 应保持不变
   - 测试累加场景（速度+战斗+Boss）

【输出要求】

```
================================================================================
Sub-Step 3.7.4 完成报告
================================================================================

【修改内容】
- 文件：CameraModule_Constraint.cpp
- 修改位置：C03模块
- 代码行数：约XX行

【实现的功能】
- ✅ ShouldActivate：始终激活
- ✅ Compute：FOV限制在MinFOV到MaxFOV

【编译结果】
- 编译状态：[成功/失败]

【最终状态】
✅ C03完成，FOV限制

================================================================================
```

【后续步骤】

C03完成后，Constraint模块进度：
- [x] ✅ 3.7.1:  CameraModule_Constraint.h
- [x] ✅ 3.7.2: C01 - Pitch Clamp
- [x] ✅ 3.7.3: C02 - Distance Clamp
- [x] ✅ 3.7.4: C03 - FOV Clamp
- [ ] ⏳ 3.7.5: C04 - Visibility Ensure（最后一个模块！）
- [ ] ⏳ 3.7.6: 系统集成与验证

**Constraint模块完成度：3/4 (75%)**

**Phase 3进度：42/43模块 = 97.7%**

================================================================================