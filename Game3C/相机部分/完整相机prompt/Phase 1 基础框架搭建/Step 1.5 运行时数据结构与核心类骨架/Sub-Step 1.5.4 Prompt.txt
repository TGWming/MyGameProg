================================================================================
任务：创建SoulsCameraStateMachine.  h - 状态机类声明
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4.        27 C++项目。
状态机管理725个相机状态的切换和参数查询。
在Stage 2中被调用，决定当前应该使用哪个状态。

【当前进度】
Phase 1 Step 1.5 Sub-Step 1.5.4 (共190个Sub-Step中的第32个)
前置完成：1.  5.1-1.5.3 运行时数据结构

【任务目标】
创建文件：Source/[ProjectName]/Camera/Core/SoulsCameraStateMachine.  h
定义USoulsCameraStateMachine类，管理状态切换和查询。

【具体要求】

1. 文件路径：Source/[ProjectName]/Camera/Core/SoulsCameraStateMachine.   h

2. 完整文件内容：

```cpp
// Copyright Your Company.          All Rights Reserved.    

#pragma once

#include "CoreMinimal.h"
#include "UObject/NoExportTypes.h"
#include "Engine/DataTable.h"
#include "SoulsCameraTypes.h"
#include "SoulsCameraConfig.h"
#include "SoulsCameraRuntimeTypes.h"
#include "SoulsCameraStateMachine. generated.h"

/**
 * USoulsCameraStateMachine
 * 
 * Manages camera state transitions and configuration lookup.    
 * Handles the 725 camera states defined in the DataTable.  
 */
UCLASS(BlueprintType)
class YOURPROJECT_API USoulsCameraStateMachine :  public UObject
{
    GENERATED_BODY()

public:
    USoulsCameraStateMachine();

    //========================================
    // Initialization
    //========================================
    
    /** Initialize with DataTable reference */
    UFUNCTION(BlueprintCallable, Category = "Camera|StateMachine")
    void Initialize(UDataTable* InStatesDataTable);

    /** Check if initialized and ready */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool IsInitialized() const;

    //========================================
    // State Management
    //========================================
    
    /** Request a state change */
    UFUNCTION(BlueprintCallable, Category = "Camera|StateMachine")
    bool RequestStateChange(FName NewStateName, bool bForce = false);

    /** Get current state name */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    FName GetCurrentStateName() const;

    /** Get previous state name */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    FName GetPreviousStateName() const;

    /** Get current state configuration */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool GetCurrentStateConfig(FCameraStateConfig& OutConfig) const;

    /** Get configuration for any state by name */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool GetStateConfig(FName StateName, FCameraStateConfig& OutConfig) const;

    /** Check if a state exists */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool DoesStateExist(FName StateName) const;

    //========================================
    // State Query
    //========================================
    
    /** Get all states in a category */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    TArray<FName> GetStatesInCategory(ECameraStateCategory Category) const;

    /** Get current state category */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    ECameraStateCategory GetCurrentCategory() const;

    /** Check if currently in specified category */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool IsInCategory(ECameraStateCategory Category) const;

    //========================================
    // Transition
    //========================================
    
    /** Update transition (called each frame) */
    void UpdateTransition(float DeltaTime);

    /** Get current blend alpha (0=previous state, 1=current state) */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    float GetBlendAlpha() const;

    /** Check if currently transitioning */
    UFUNCTION(BlueprintPure, Category = "Camera|StateMachine")
    bool IsInTransition() const;

    /** Get blended config between previous and current state */
    bool GetBlendedConfig(FCameraStateConfig& OutConfig) const;

    //========================================
    // Evaluation
    //========================================
    
    /** Evaluate and potentially change state based on input context */
    void EvaluateState(const FCameraInputContext& Context);

    //========================================
    // Events (Delegates)
    //========================================
    
    /** Called when state changes */
    DECLARE_DYNAMIC_MULTICAST_DELEGATE_TwoParams(FOnCameraStateChanged, FName, OldState, FName, NewState);
    
    UPROPERTY(BlueprintAssignable, Category = "Camera|StateMachine")
    FOnCameraStateChanged OnStateChanged;

    /** Called when transition completes */
    DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnCameraTransitionComplete, FName, NewState);
    
    UPROPERTY(BlueprintAssignable, Category = "Camera|StateMachine")
    FOnCameraTransitionComplete OnTransitionComplete;

protected:
    //========================================
    // Internal State
    //========================================
    
    /** Reference to states DataTable */
    UPROPERTY()
    UDataTable* StatesDataTable;

    /** Current state name */
    UPROPERTY()
    FName CurrentStateName;

    /** Previous state name (for blending) */
    UPROPERTY()
    FName PreviousStateName;

    /** Current blend alpha */
    UPROPERTY()
    float BlendAlpha;

    /** Time remaining in transition */
    UPROPERTY()
    float TransitionTimeRemaining;

    /** Total transition time */
    UPROPERTY()
    float TransitionTotalTime;

    /** Current blend type */
    UPROPERTY()
    ECameraBlendType CurrentBlendType;

    /** Is initialized */
    UPROPERTY()
    bool bIsInitialized;

    /** Cached state configs for quick lookup */
    TMap<FName, FCameraStateConfig> CachedConfigs;

    //========================================
    // Internal Methods
    //========================================
    
    /** Cache all state configs from DataTable */
    void CacheStateConfigs();

    /** Calculate blend alpha based on blend type */
    float CalculateBlendAlpha(float LinearAlpha, ECameraBlendType BlendType) const;

    /** Apply blend function */
    static float ApplyBlendCurve(float Alpha, ECameraBlendType BlendType);
};
```

3. 代码风格：
   - UCLASS(BlueprintType)使其可在蓝图中使用
   - 提供完整的状态管理接口
   - 使用委托通知状态变化
   - 缓存配置以提高查询性能
   - YOURPROJECT_API需要替换为实际项目API宏

【验证标准】
- 编译通过（需要配合.     cpp文件）
- 类可在蓝图中看到
- 委托可在蓝图中绑定

【注意事项】
- 这只是头文件，需要配合.  cpp实现
- 下一个Sub-Step将创建.     cpp

【输出要求】
输出完整的SoulsCameraStateMachine.  h文件代码。

================================================================================
                        上下文限制与自动交接机制
================================================================================

【重要：上下文限制检测】

在执行任务过程中，请持续监控上下文窗口使用情况。

如果你检测到以下任一情况：
1. 上下文窗口即将达到限制（约80%容量）
2. 无法在当前对话中完成全部任务
3. 需要生成的代码量超出剩余空间

请立即执行以下操作：

【自动交接协议】

停止当前代码生成，输出以下格式的交接信息：

```
================================================================================
⚠️ 上下文限制 - 自动交接点
================================================================================

【交接状态】
- 当前任务：Sub-Step 1.5.4 SoulsCameraStateMachine. h (状态机类声明)
- 完成进度：[百分比] 或 [具体进度描述]
- 中断位置：[具体说明在哪里中断，例如：完成到Transition部分，未完成Evaluation和Events]

【已完成内容】
- 文件头部（#pragma once, includes）
- UCLASS声明开始
- [列出已完成的函数分组]

【未完成内容】
- [列出剩余函数分组]
- protected成员变量
- Internal Methods

【已生成代码保存提示】
请将上方已生成的代码保存到新文件SoulsCameraStateMachine.h，然后开启新对话。

================================================================================
                    继续任务 - 新对话PROMPT
================================================================================

请复制以下内容到新对话继续执行：

---开始复制---

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
Phase 1 Step 1.5 Sub-Step 1.5.4 续接任务。
创建USoulsCameraStateMachine类声明（状态机管理725个状态）。

【前置完成】
- Step 1.1-1.4 所有参数结构体和配置 ✅
- Sub-Step 1.5.1 FCameraInputContext ✅
- Sub-Step 1.5.2 FSoulsCameraOutput ✅
- Sub-Step 1.5.3 FModuleOutput/FModifierOutput ✅
- Sub-Step 1.5.4 部分完成：
  - 文件头部 ✅
  - UCLASS声明开始 ✅
  - [列出已完成的函数分组] ✅

【续接任务】
继续完成SoulsCameraStateMachine.h文件：
- [列出剩余函数分组]
- protected成员变量
- Internal Methods
- 类定义结束

【已有代码上下文】
文件：SoulsCameraStateMachine.h（已创建，需要继续添加）
USoulsCameraStateMachine类已开始定义，继承自UObject。

【USoulsCameraStateMachine类结构要求】
public函数分组：
- Initialization (2个)：Initialize(), IsInitialized()
- State Management (6个)：RequestStateChange(), GetCurrentStateName(), GetPreviousStateName(), GetCurrentStateConfig(), GetStateConfig(), DoesStateExist()
- State Query (3个)：GetStatesInCategory(), GetCurrentCategory(), IsInCategory()
- Transition (4个)：UpdateTransition(), GetBlendAlpha(), IsInTransition(), GetBlendedConfig()
- Evaluation (1个)：EvaluateState()
- Events (2个委托)：OnStateChanged, OnTransitionComplete

protected成员：
- StatesDataTable, CurrentStateName, PreviousStateName
- BlendAlpha, TransitionTimeRemaining, TransitionTotalTime
- CurrentBlendType, bIsInitialized
- CachedConfigs (TMap)

Internal Methods (3个)：
- CacheStateConfigs(), CalculateBlendAlpha(), ApplyBlendCurve()

【输出要求】
继续生成剩余代码，完成SoulsCameraStateMachine.h文件。

【上下文限制检测】
本任务仍需遵循上下文限制检测机制。如再次达到限制，请再次生成交接信息。

---结束复制---

================================================================================
```

【交接信息要求】
1. 交接PROMPT必须是**完全自包含**的，新对话无需任何额外信息即可继续
2. 包含所有必要的**代码上下文**（关键结构体定义、依赖关系等）
3. 明确标注**中断位置**和**剩余任务**
4. 标明目标文件：SoulsCameraStateMachine.  h
5. 这只是头文件，下一个Sub-Step将创建.    cpp实现
6. 列出所有函数分组和成员变量
7. 标明YOURPROJECT_API需要替换为实际项目API宏

================================================================================