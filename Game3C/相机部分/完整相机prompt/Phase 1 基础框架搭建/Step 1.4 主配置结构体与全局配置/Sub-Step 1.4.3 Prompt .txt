================================================================================
任务：创建SoulsCameraGlobalConfig.h - 全局配置UObject类声明
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4.  27 C++项目。
创建全局配置的UObject类，用于存储E组全局参数。
这个类可以作为DataAsset在编辑器中创建和编辑。

【当前进度】
Phase 1 Step 1.4 Sub-Step 1.4. 3 (共190个Sub-Step中的第27个)
前置完成：
- 1. 4.1 SoulsCameraParams_Global.  h (E组参数结构体)
- 1.4.2 SoulsCameraConfig. h (主配置结构体)

【任务目标】
创建文件：Source/[ProjectName]/Camera/Core/SoulsCameraGlobalConfig.  h
定义USoulsCameraGlobalConfig类，用于管理全局相机设置。

【具体要求】

1. 文件路径：Source/[ProjectName]/Camera/Core/SoulsCameraGlobalConfig. h

2. 完整文件内容：

```cpp
// Copyright Your Company.     All Rights Reserved.  

#pragma once

#include "CoreMinimal.h"
#include "UObject/NoExportTypes.h"
#include "Engine/DataAsset.h"
#include "SoulsCameraParams_Global.h"
#include "SoulsCameraGlobalConfig.generated.h"

/**
 * USoulsCameraGlobalConfig
 * 
 * Data asset containing global camera system settings.   
 * Create instances of this in the editor via:  
 * Content Browser -> Add New -> Miscellaneous -> Data Asset -> SoulsCameraGlobalConfig
 * 
 * This contains system-wide settings that apply across all camera states,
 * including limits, smoothing, transitions, debug, lock-on, and performance settings. 
 */
UCLASS(BlueprintType, Blueprintable)
class YOURPROJECT_API USoulsCameraGlobalConfig : public UDataAsset
{
    GENERATED_BODY()

public:  
    USoulsCameraGlobalConfig();

    //========================================
    // Global Parameters (E-Group, 22 params)
    //========================================
    
    /** All global camera system parameters */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Global Settings")
    FGlobalParams GlobalParams;

    //========================================
    // DataTable Reference
    //========================================
    
    /** Reference to the camera states DataTable */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Data References")
    TSoftObjectPtr<UDataTable> StatesDataTable;

    //========================================
    // Helper Functions
    //========================================
    
    /** Get global distance limits */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    void GetDistanceLimits(float& OutMin, float& OutMax) const;

    /** Get global FOV limits */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    void GetFOVLimits(float& OutMin, float& OutMax) const;

    /** Get default blend settings */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    void GetDefaultBlendSettings(float& OutTime, ECameraBlendType& OutType) const;

    /** Check if debug is enabled by default */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    bool IsDebugEnabledByDefault() const;

    /** Get lock-on settings */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    float GetMaxLockOnDistance() const;

    /** Get camera update rate (0 = every frame) */
    UFUNCTION(BlueprintPure, Category = "Camera|Global")
    int32 GetUpdateRate() const;

    //========================================
    // Validation
    //========================================
    
#if WITH_EDITOR
    /** Validate configuration in editor */
    virtual void PostEditChangeProperty(FPropertyChangedEvent& PropertyChangedEvent) override;
#endif
};
```

3. 代码风格：
   - 继承UDataAsset便于在编辑器创建资产
   - 提供BlueprintPure函数便于蓝图访问
   - 使用TSoftObjectPtr引用DataTable避免强制加载
   - WITH_EDITOR保护编辑器专用代码

【验证标准】
- 编译通过（注意需要同时创建.  cpp文件）
- 可在编辑器中创建GlobalConfig资产
- 蓝图中可访问全局设置

【注意事项】
- YOURPROJECT_API需要替换为实际项目的API宏
- 这只是头文件，需要配合. cpp文件
- 下一个Sub-Step将创建. cpp实现

【输出要求】
输出完整的SoulsCameraGlobalConfig.  h文件代码。

================================================================================
                        上下文限制与自动交接机制
================================================================================

【重要：上下文限制检测】

在执行任务过程中，请持续监控上下文窗口使用情况。

如果你检测到以下任一情况：
1. 上下文窗口即将达到限制（约80%容量）
2. 无法在当前对话中完成全部任务
3. 需要生成的代码量超出剩余空间

请立即执行以下操作：

【自动交接协议】

停止当前代码生成，输出以下格式的交接信息：

```
================================================================================
⚠️ 上下文限制 - 自动交接点
================================================================================

【交接状态】
- 当前任务：Sub-Step 1.4.3 SoulsCameraGlobalConfig. h (全局配置UObject类声明)
- 完成进度：[百分比] 或 [具体进度描述]
- 中断位置：[具体说明在哪里中断，例如：完成到Helper Functions，未完成Validation部分]

【已完成内容】
- 文件头部（#pragma once, includes）
- UCLASS声明
- [列出已完成的UPROPERTY和UFUNCTION]

【未完成内容】
- [列出剩余UFUNCTION]
- WITH_EDITOR验证代码
- 类定义结束

【已生成代码保存提示】
请将上方已生成的代码保存到新文件SoulsCameraGlobalConfig.  h，然后开启新对话。

================================================================================
                    继续任务 - 新对话PROMPT
================================================================================

请复制以下内容到新对话继续执行：

---开始复制---

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
Phase 1 Step 1.4 Sub-Step 1.4.3 续接任务。
创建USoulsCameraGlobalConfig类（全局配置UObject）。

【前置完成】
- Step 1.1-1.3 所有参数结构体 ✅
- Step 1.4.1 SoulsCameraParams_Global. h (E组22参数) ✅
- Step 1.4.2 SoulsCameraConfig.h (FCameraStateConfig) ✅
- Sub-Step 1.4.3 部分完成：
  - 文件头部 ✅
  - UCLASS声明 ✅
  - [列出已完成的成员] ✅

【续接任务】
继续完成SoulsCameraGlobalConfig. h文件：
- [列出剩余UFUNCTION]
- WITH_EDITOR验证代码
- 类定义结束

【已有代码上下文】
文件：SoulsCameraGlobalConfig.h（已创建，需要继续添加）
USoulsCameraGlobalConfig类已开始定义，继承自UDataAsset。

【USoulsCameraGlobalConfig类结构要求】
- 继承UDataAsset
- UPROPERTY成员：
  - FGlobalParams GlobalParams
  - TSoftObjectPtr<UDataTable> StatesDataTable
- UFUNCTION BlueprintPure函数（6个）：
  - GetDistanceLimits()
  - GetFOVLimits()
  - GetDefaultBlendSettings()
  - IsDebugEnabledByDefault()
  - GetMaxLockOnDistance()
  - GetUpdateRate()
- WITH_EDITOR验证：
  - PostEditChangeProperty()

【输出要求】
继续生成剩余代码，完成SoulsCameraGlobalConfig.h文件。

【上下文限制检测】
本任务仍需遵循上下文限制检测机制。如再次达到限制，请再次生成交接信息。

---结束复制---

================================================================================
```

【交接信息要求】
1. 交接PROMPT必须是**完全自包含**的，新对话无需任何额外信息即可继续
2. 包含所有必要的**代码上下文**（关键结构体定义、依赖关系等）
3. 明确标注**中断位置**和**剩余任务**
4. 标明目标文件：SoulsCameraGlobalConfig.  h
5. 这只是头文件，下一个Sub-Step将创建.  cpp实现
6. 标明继承关系：UDataAsset
7. 列出所有需要实现的UFUNCTION（共6个）

================================================================================