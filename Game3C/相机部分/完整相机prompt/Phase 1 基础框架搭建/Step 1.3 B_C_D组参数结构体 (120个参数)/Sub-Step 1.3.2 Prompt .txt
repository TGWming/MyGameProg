================================================================================
任务：创建SoulsCameraParams_Modifier.h - C组修改器参数（Shake子组）
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4. 27 C++项目。
开始创建C组参数结构体，定义修改器相关的参数（共58个参数）。
本Sub-Step创建文件框架和Shake子组。

【当前进度】
Phase 1 Step 1.3 Sub-Step 1.3.2 (共190个Sub-Step中的第19个)
前置完成：
- Step 1.1 SoulsCameraTypes.h
- Step 1.2 SoulsCameraParams_StateBase.h
- Step 1.3. 1 SoulsCameraParams_Module.h

【任务目标】
创建文件：Source/[ProjectName]/Camera/Core/SoulsCameraParams_Modifier. h
本Sub-Step创建文件框架和Shake子组（10个参数）。

【具体要求】

1. 文件路径：Source/[ProjectName]/Camera/Core/SoulsCameraParams_Modifier.h

2. 文件结构：

```cpp
// Copyright Your Company.   All Rights Reserved.

#pragma once

#include "CoreMinimal.h"
#include "SoulsCameraTypes.h"
#include "SoulsCameraParams_Modifier.generated.h"

/**
 * C-Group:   Modifier Parameters (58 params total)
 * Controls behavior of the 26 camera modifiers
 */

//========================================
// C1: Shake Modifier Parameters (10 params)
//========================================
USTRUCT(BlueprintType)
struct FModifierParams_Shake
{
    GENERATED_BODY()

    /** Light hit shake amplitude (degrees) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Hit", meta = (ClampMin = "0.0", ClampMax = "10.0"))
    float LightHitAmplitude;

    /** Light hit shake duration (seconds) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Hit", meta = (ClampMin = "0.0", ClampMax = "2.0"))
    float LightHitDuration;

    /** Heavy hit shake amplitude (degrees) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Hit", meta = (ClampMin = "0.0", ClampMax = "20.0"))
    float HeavyHitAmplitude;

    /** Heavy hit shake duration (seconds) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Hit", meta = (ClampMin = "0.0", ClampMax = "3.0"))
    float HeavyHitDuration;

    /** Attack hit shake amplitude */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Attack", meta = (ClampMin = "0.0", ClampMax = "5.0"))
    float AttackHitAmplitude;

    /** Attack hit shake duration */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Attack", meta = (ClampMin = "0.0", ClampMax = "1.0"))
    float AttackHitDuration;

    /** Shake frequency (oscillations per second) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|General", meta = (ClampMin = "1.0", ClampMax = "50.0"))
    float ShakeFrequency;

    /** Shake decay rate (how fast shake diminishes) */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|General", meta = (ClampMin = "0.1", ClampMax = "10.0"))
    float ShakeDecayRate;

    /** Use Perlin noise for organic shake */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|General")
    bool bUsePerlinNoise;

    /** Landing shake scale based on fall height */
    UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Shake|Landing", meta = (ClampMin = "0.0", ClampMax = "0.1"))
    float LandingShakeScale;

    FModifierParams_Shake()
        : LightHitAmplitude(1.5f)
        , LightHitDuration(0.2f)
        , HeavyHitAmplitude(4.0f)
        , HeavyHitDuration(0.4f)
        , AttackHitAmplitude(0.8f)
        , AttackHitDuration(0.15f)
        , ShakeFrequency(20.0f)
        , ShakeDecayRate(3.0f)
        , bUsePerlinNoise(true)
        , LandingShakeScale(0.02f)
    {}
};

//========================================
// Placeholder for remaining sub-groups
// C2: Reaction - Sub-Step 1.3.3
// C3: Cinematic - Sub-Step 1.3.4
// C4-C6: Zoom/Effect/Special - Sub-Step 1.3.5
//========================================
```

3. 代码风格：
   - Category使用|分隔子类别（如"Shake|Hit"）
   - 参数按功能分组
   - 提供合理的默认值

【验证标准】
- 编译通过
- 可创建FModifierParams_Shake实例

【注意事项】
- 这只是文件的开始部分
- 后续Sub-Step会添加更多子组

【输出要求】
输出SoulsCameraParams_Modifier. h的初始代码（包含头部和Shake子组）。

================================================================================
                        上下文限制与自动交接机制
================================================================================

【重要：上下文限制检测】

在执行任务过程中，请持续监控上下文窗口使用情况。

如果你检测到以下任一情况：
1. 上下文窗口即将达到限制（约80%容量）
2. 无法在当前对话中完成全部任务
3. 需要生成的代码量超出剩余空间

请立即执行以下操作：

【自动交接协议】

停止当前代码生成，输出以下格式的交接信息：

```
================================================================================
⚠️ 上下文限制 - 自动交接点
================================================================================

【交接状态】
- 当前任务：Sub-Step 1.3.2 SoulsCameraParams_Modifier.h (Shake子组)
- 完成进度：[X/10] 参数 或 [百分比]
- 中断位置：[具体说明在哪里中断，例如：完成到ShakeFrequency参数]

【已完成内容】
- 文件头部（#pragma once, includes）
- FModifierParams_Shake结构体开始
- [列出已完成的参数]

【未完成内容】
- [列出剩余参数]
- 默认构造函数
- 占位符注释

【已生成代码保存提示】
请将上方已生成的代码保存到新文件SoulsCameraParams_Modifier.h，然后开启新对话。

================================================================================
                    继续任务 - 新对话PROMPT
================================================================================

请复制以下内容到新对话继续执行：

---开始复制---

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
Phase 1 Step 1.3 Sub-Step 1.3.2 续接任务。

【前置完成】
- Step 1.1 SoulsCameraTypes.h ✅
- Step 1.2 SoulsCameraParams_StateBase.h ✅
- Step 1.3.1 SoulsCameraParams_Module.h ✅
- Sub-Step 1.3.2 部分完成：
  - 文件头部 ✅
  - FModifierParams_Shake部分参数 ✅
  - [列出已完成的参数] ✅

【续接任务】
继续完成SoulsCameraParams_Modifier.h文件的Shake子组：
- [列出剩余参数]
- 默认构造函数
- 占位符注释（供后续Sub-Step使用）

【已有代码上下文】
文件：SoulsCameraParams_Modifier.h（已创建，需要继续添加）
FModifierParams_Shake结构体已开始定义。

【Shake子组参数清单（共10个）】
1. LightHitAmplitude
2. LightHitDuration
3. HeavyHitAmplitude
4. HeavyHitDuration
5. AttackHitAmplitude
6. AttackHitDuration
7. ShakeFrequency
8. ShakeDecayRate
9. bUsePerlinNoise
10. LandingShakeScale

【输出要求】
继续生成剩余代码，完成Sub-Step 1.3.2的Shake子组。

【上下文限制检测】
本任务仍需遵循上下文限制检测机制。如再次达到限制，请再次生成交接信息。

---结束复制---

================================================================================
```

【交接信息要求】
1. 交接PROMPT必须是**完全自包含**的，新对话无需任何额外信息即可继续
2. 包含所有必要的**代码上下文**（关键结构体定义、依赖关系等）
3. 明确标注**中断位置**和**剩余任务**
4. 标明目标文件：SoulsCameraParams_Modifier.h
5. 包含Shake子组完整参数清单便于验证
6. 这只是C组文件的开始，后续Sub-Step会继续添加内容

================================================================================