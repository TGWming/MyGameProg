================================================================================
任务：创建SoulsCameraTypes.h - 前向声明与基础头文件
================================================================================

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
这是整个相机系统的第一个文件，定义所有枚举类型和前向声明。

【当前进度】
Phase 1 Step 1.1 Sub-Step 1.1.1 (共190个Sub-Step中的第1个)

【任务目标】
创建文件：Source/[ProjectName]/Camera/Core/SoulsCameraTypes.h
本Sub-Step只创建文件框架、版权声明、pragma once、必要的UE4头文件包含和前向声明部分。
枚举定义将在后续Sub-Step中添加。

【具体要求】

1. 文件路径：Source/[ProjectName]/Camera/Core/SoulsCameraTypes.h

2. 文件内容必须包含：
   - 版权声明注释块
   - #pragma once
   - 必要的UE4头文件：
     * CoreMinimal. h
     * UObject/ObjectMacros.h
     * SoulsCameraTypes.generated.h (最后一个include)
   
3. 前向声明部分（class前向声明）：
   - class USoulsCameraManager;
   - class USoulsCameraPipeline;
   - class USoulsCameraStateMachine;
   - class UCameraModuleBase;
   - class UCameraModuleRegistry;
   - class UCameraModifierBase;
   - class UCameraModifierManager;
   - class UCameraCollisionResolver;
   - class USpringArmComponent;
   - class UCameraComponent;

4. 预留枚举定义区域的注释占位符：
   //========================================
   // 枚举定义 - 将在后续Sub-Step中添加
   //========================================

5. 代码风格：
   - 使用UE4标准代码风格
   - 注释使用英文
   - 适当的空行分隔各部分

【验证标准】
- 文件创建成功
- 编译通过（0 errors, 0 warnings优先，可接受的warnings除外）
- 可在其他文件中#include "SoulsCameraTypes.h"

【注意事项】
- 这是Layer 0文件，不依赖项目中的任何其他自定义头文件
- 只依赖UE4引擎头文件
- generated.h必须是最后一个#include
- **添加新文件后需要重新Generate Visual Studio project files**

【输出要求】
只生成SoulsCameraTypes. h的完整代码，不要生成其他文件。

================================================================================
                        上下文限制与自动交接机制
================================================================================

【重要：上下文限制检测】

在执行任务过程中，请持续监控上下文窗口使用情况。

如果你检测到以下任一情况：
1. 上下文窗口即将达到限制（约80%容量）
2. 无法在当前对话中完成全部任务
3. 需要生成的代码量超出剩余空间

请立即执行以下操作：

【自动交接协议】

停止当前代码生成，输出以下格式的交接信息：

```
================================================================================
⚠️ 上下文限制 - 自动交接点
================================================================================

【交接状态】
- 当前任务：Sub-Step 1.1.1 SoulsCameraTypes. h (前向声明与基础头文件)
- 完成进度：[百分比] 或 [具体进度描述]
- 中断位置：[具体说明在哪里中断，例如：完成到前向声明，未完成枚举占位符]

【已完成内容】
- 版权声明
- #pragma once
- [列出已完成的部分]

【未完成内容】
- [列出剩余部分]

【已生成代码保存提示】
请将上方已生成的代码保存到新文件SoulsCameraTypes. h，然后开启新对话。

================================================================================
                    继续任务 - 新对话PROMPT
================================================================================

请复制以下内容到新对话继续执行：

---开始复制---

【项目背景】
类魂游戏第三人称相机系统，UE4.27 C++项目。
Phase 1 Step 1.1 Sub-Step 1.1.1 续接任务。
创建SoulsCameraTypes.h基础头文件。
这是整个相机系统的第一个文件（共190个Sub-Step中的第1个）。

【前置完成】
- Sub-Step 1.1.1 部分完成：
  - [列出已完成的部分] ✅

【续接任务】
继续完成SoulsCameraTypes. h文件：
- [列出剩余部分]

【已有代码上下文】
文件：SoulsCameraTypes. h（已创建，需要继续添加）

【SoulsCameraTypes. h文件结构要求】
- 版权声明注释块
- #pragma once
- UE4头文件包含：CoreMinimal.h, UObject/ObjectMacros.h, SoulsCameraTypes.generated.h
- 前向声明（10个类）
- 枚举定义占位符注释

【输出要求】
继续生成剩余代码，完成SoulsCameraTypes. h文件。

【编译注意】
添加新. h文件后需要：
1. 右键. uproject → Generate Visual Studio project files
2. 重新打开. sln或Reload All
3. 编译

【上下文限制检测】
本任务仍需遵循上下文限制检测机制。如再次达到限制，请再次生成交接信息。

---结束复制---

================================================================================
```

【交接信息要求】
1. 交接PROMPT必须是**完全自包含**的，新对话无需任何额外信息即可继续
2. 包含所有必要的**代码上下文**（关键结构体定义、依赖关系等）
3. 明确标注**中断位置**和**剩余任务**
4. 标明目标文件：SoulsCameraTypes. h
5. 这是Phase 1 Step 1.1的第一个Sub-Step
6. 列出所有需要的前向声明（10个类）
7. 标明这是Layer 0文件，不依赖其他自定义头文件

================================================================================