================================================================================
任务：在 TargetDetectionComponent.cpp 和 UIManagerComponent.cpp 中添加埋点
================================================================================

【项目背景】
FlowVisualizer 是一个 UE 4.27 编辑器插件，用于在编辑器独立弹出窗口中
以终端风格可视化观察运行时数据流。
插件采用双模块结构：FlowVisualizerRuntime + FlowVisualizerEditor。

【当前进度】
Phase 8 P8.3（共 32 个原子化任务中的第 27 个）

前置完成：
- Phase 1 ~ Phase 7 全部完成 ✅
- P8.1 soul.Build.cs 已添加 FlowVisualizerRuntime 依赖 ✅
  （游戏代码可 #include "FlowTracerMacros.h"）
- P8.2 MyCharacter.cpp 埋点已完成 ✅

注意：P8.3 的直接依赖仅为 P8.1（Build.cs 依赖），
与 P8.2 无直接依赖关系，两者可并行。

【安全约束（所有任务必须遵守）】
1. ★ Phase 8 允许修改 Soul 项目游戏源文件
2. 禁止修改项目 .uproject 文件
3. 禁止引入第三方库；仅使用 UE 4.27 原生 API
4. 所有 FLOW_SIGNAL 宏在 Shipping 构建��自动展开为空（零开销）
5. 不产生任何 UI 到游戏视口
6. 每个 Task 结束后必须能编译通过且不影响现有项目
7. ★ 关键：仅添加 FLOW_SIGNAL 调用行，不修改任何原有游戏逻辑

【原子化任务独立性宪法提醒】
本 Prompt 仅涉及任务 P8.3 的内容。
不要执行任何超出 P8.3 范围的工作。
不要创建 SoulFlowSetup 文件（那是 P8.4）。
不要修改 MyCharacter.cpp（P8.2 已完成）。

【任务目标】
在 Soul 项目的两个组件类源文件中分别添加 FLOW_SIGNAL 埋点，
监控锁定目标检测和 UI 管理器的关键事件。
每处埋点仅一行代码，不修改任何原有逻辑。

【具体要求】

1. 需要修改的文件：

   文件 A：TargetDetectionComponent.cpp
   （位于 Soul 项目的 Private 目录下）

   文件 B：UIManagerComponent.cpp
   （位于 Soul 项目的 Private 目录下）

2. TargetDetectionComponent.cpp 修改：

   顶部添加 include：
   - #include "FlowTracerMacros.h"
   - 放在其他 #include 之后

   埋点位置 — FindLockOnCandidates()（或类似名称的候选目标查找函数）：
   - 位置：函数体入口处（或候选列表生成之后）
   - 代码：
     FLOW_SIGNAL_FMT("TargetDetection", "Candidates=%d",
         LockOnCandidates.Num());
   - 注意：LockOnCandidates 变量名需适配实际代码
     IDE Agent 应先读取文件确认实际变量名和函数名
   - 如果函数名不同（如 DetectTargets / ScanForTargets 等），
     在对应函数中埋点

3. UIManagerComponent.cpp 修改：

   顶部添加 include：
   - #include "FlowTracerMacros.h"
   - 放在其他 #include 之后

   埋点位置 — ShowLockOnWidget(AActor* Target)（或类似名称的锁定 UI 显示函数）：
   - 位置：函数体入口处
   - 代码：
     FLOW_SIGNAL_FMT("UIManager", "Target=%s",
         Target ? *Target->GetName() : TEXT("None"));
   - 注意：函数名和参数名需适配实际代码
     IDE Agent 应先读取文件确认
   - 如果函数名不同（如 DisplayLockOnUI / UpdateLockOnWidget 等），
     在对应函数中埋点

4. 关键说明：
   - 以上埋点中的函数名和变量名是基于设计文档的预估名称
   - IDE Agent 执行时应先读取两个 .cpp 文件的现有内容，
     确认实际函数签名和变量名，然后适配
   - 如果某个文件不存在或某个函数不存在：
     在该文件中仅添加 #include 行即可，跳过不存在的埋点
     并留注释说明：// TODO: 添加 FLOW_SIGNAL 埋点到 [函数名]
   - 每个 FLOW_SIGNAL 调用占一行

【此任务不做的事情】
- 不创建 SoulFlowSetup 管线注册文件（那是 P8.4）
- 不修改 MyCharacter.cpp（P8.2 已完成）
- 不修改 Build.cs（P8.1 已完成）
- 不修改任何原有游戏逻辑

【验证标准】
- 编译通过
- TargetDetectionComponent.cpp 顶部包含 #include "FlowTracerMacros.h"
- UIManagerComponent.cpp 顶部包含 #include "FlowTracerMacros.h"
- 对应函数中有 FLOW_SIGNAL 调用（如果函数存在）
- 不影响原有游戏逻辑
- PIE 运行锁定目标时，FFlowTracer 收到 TargetDetection 和 UIManager 信号
- 未修改本任务范围外的任何文件

================================================================================
                    上下文限制与自动交接机制
================================================================================

【重要：上下文限制检测】

在执行任务过程中，请持续监控上下文窗口使用情况。

如果你检测到以下任一情况：
1. 上下文窗口即将达到限制（约80%容量）
2. 无法在当前对话中完成全部任务
3. 需要生成的代码量超出剩余空间

请立即执行以下操作：

【自动交接协议】

停止当前代码生成，输出以下格式的交接信息：

================================================================================
⚠️ 上下文限制 - 自动交接点
================================================================================

【交接状态】
- 当前任务：P8.3 组件类埋点
- 完成进度：[百分比] 或 [具体进度描述]
- 中断位置：[具体说明在哪里中断]

【已完成内容】
- [列出已完成的部分]

【未完成内容】
- [列出剩余部分]

【已生成代码保存提示】
请将上方已生��的代码保存，然后开启新对话。

================================================================================
                    继续任务 - 新对话 PROMPT
================================================================================

请复制以下内容到新对话继续执行：

---开始复制---

【项目背景】
FlowVisualizer UE 4.27 编辑器插件，终端风格数据流可视化。
Phase 8 P8.3 续接任务。

【前置完成】
- Phase 1 ~ Phase 7 全部完成 ✅
- P8.1 Build.cs 依赖已添加 ✅
- P8.2 MyCharacter 埋点已完成 ✅
- P8.3 部分完成：
  - [列出已完成的部分] ✅

【续接任务】
继续完成组件类埋点。
修改文件：TargetDetectionComponent.cpp + UIManagerComponent.cpp
需先读取文件确认实际函数名和变量名。

【上下文限制检测】
本任务仍需遵循上下文限制检测机制。

---结束复制---

================================================================================